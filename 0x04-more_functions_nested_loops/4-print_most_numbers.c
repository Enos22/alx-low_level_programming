#include "main.h"

/**
 * print_most_numbers - prints all numbers except 2 and 4
 * @c: intager value
 * return: 0 on success
 */
void print_most_numbers(void)
{
	int c;

	for (c = 0; c < 10; i++)
	{
		if (c != 2 && c != 4)

		_putchar(c + '0');
	}
	_putchar('\n');
}
