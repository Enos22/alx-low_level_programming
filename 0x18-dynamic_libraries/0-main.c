#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - check the code
 * _strlen - defines the lenght of string
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
    printf("%d\n", _strlen("My Dyn Lib"));
    return (EXIT_SUCCESS);
}
